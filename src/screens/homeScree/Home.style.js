import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    textStyle: {
     fontSize: 20,
     fontWeight: 'bold',
     paddingHorizontal: 10,
     paddingVertical: 10
    },
  });
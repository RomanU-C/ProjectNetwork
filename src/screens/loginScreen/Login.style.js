import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    loginContainer: {
     flex: 1,
     alignItems: 'center',
     backgroundColor: '#696969'
    },
    input: {
        width: '60%',
        height: 40,
        margin: 12,
        borderWidth: 1,
        color: '#000',
      },
  });